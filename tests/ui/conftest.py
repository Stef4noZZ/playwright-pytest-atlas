"""UI-only fixtures.

Cross-cutting fixtures (page objects, API clients used by e2e) live in
`tests/conftest.py`. Put fixtures here only when they should never be visible
to API-only or e2e tests.
"""
